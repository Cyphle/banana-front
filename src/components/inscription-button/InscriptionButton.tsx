import { Button } from 'antd';

export interface InscriptionButtonProps {

}

export const InscriptionButton = (_: InscriptionButtonProps) => {
  return (
    <div className="cta">
      <Button type="primary">Blbla</Button>
    </div>
  )
}